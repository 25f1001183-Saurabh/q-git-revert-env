name: CI Visibility

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  ci-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run CI visibility check
        run: echo "CI workflow ran successfully"

